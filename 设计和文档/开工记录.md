# OpenGameTranslator 开工记录

这份文档用于在新窗口快速恢复项目上下文。优先记录项目目标、当前决策、已完成工作和下一步任务，不记录本地逆向报告的具体内容。

## 1. 项目目标

OpenGameTranslator 是一个开源游戏翻译 CLI 工具。

核心目标：

- 不内置翻译 API。
- 不做自动翻译。
- 不直接破坏性修改游戏源文件。
- 支持文本提取、JSONL 导出、译文校验、翻译包构建、运行时 Overlay。
- 引擎适配器必须和 Core 层解耦。
- 代码要新手友好，方便有少量 C# 经验的维护者参与。

第一阶段目标是做最小可维护架构和项目骨架，不做大而全框架。

## 2. 技术和风格约束

技术栈：

- TypeScript + Node.js。
- CLI 工具。
- strict TypeScript，并尽量开启更严格的类型检查。
- 不做 GUI。
- 运行时插件可以是普通 JavaScript。

代码风格：

- 偏 C# / .NET 风格的 TypeScript。
- 优先使用 `class`、`interface`、`readonly`、`public`、`private`。
- 显式返回类型。
- 少用复杂箭头函数和匿名函数链式调用。
- 避免 `reduce`。
- 复杂逻辑拆成具名方法。
- 可以多用 `for...of`。
- 不做复杂 DI 容器。
- 不做抽象工厂套抽象工厂。
- 不滥用 `any`；未知 JSON 优先用 `unknown`，再做类型判断。
- 代码里面需要有适量注释，帮助新手理解关键思路；不要逐行解释，也不要堆太多注释。

## 3. 计划中的最小架构

建议目录分层：

```text
src/
    core/
    engines/
        tyrano/
        rpgmaker-mv/
        rpgmaker-mz/
    cli/
runtime/
    tyrano/
    rpgmaker-mv/
    rpgmaker-mz/
```

Core 层负责引擎通用逻辑：

- `ExtractedTextEntry` 数据模型。
- `TranslationEntry` 数据模型。
- JSONL 读写。
- 控制码和占位符校验。
- 翻译包构建。
- `Result<T>` 错误处理模型。
- 日志输出。

Engine Adapter 层负责具体引擎：

- 检测游戏是否属于该引擎。
- 提取该引擎文本。
- 提供运行时 Hook 文件模板。
- 提供该引擎需要保留的控制码规则。

CLI 层只负责命令入口：

- `detect`
- `extract`
- `validate`
- `build`

Runtime 层原则：

- 找到合法译文则显示译文。
- 找不到译文则显示原文。
- 译文校验失败则显示原文。
- 出现异常则显示原文。

## 4. 当前关键决策

- 不内置翻译 API，这是明确边界。
- Runtime 只读取已经构建好的本地翻译包，不调用翻译服务。
- 翻译失败、未命中或运行时异常时，必须回退原文。
- Core 层不能依赖任何具体游戏引擎。
- 第一版优先实现 TyranoScript 适配器。
- 本地逆向报告只作为开发参考，不提交到 git 仓库。
- 类型检查按严格标准设计，让编译器尽量提前暴露问题。

## 5. 已完成

仓库和远程：

- 已初始化 git 仓库。
- 当前分支是 `main`。
- 已配置远程仓库：`git@github.com:zzzdajb/OpenGameTranslator.git`。
- 已完成首次推送。
- 初始提交不包含本地逆向报告。

开发环境：

- 已创建 `package.json`。
- 已创建 `package-lock.json`。
- 已创建 `tsconfig.json`。
- 已创建 `.gitignore`。
- 已创建 `.editorconfig`。
- 已安装 TypeScript 开发依赖。
- 已创建最小 CLI 入口：`src/cli/index.ts`。

验证：

- `npm run typecheck` 已通过。
- `npm run build` 已通过。
- `npm run dev` 已通过。

文档：

- 已保留原始需求文档：`设计和文档/初始化.txt`。
- 本文档用于恢复开工上下文。

## 6. 还没做

架构骨架：

- 未创建 `src/core/`。
- 未创建 `src/engines/`。
- 未创建 `runtime/`。
- 未定义 Core 数据模型。
- 未定义 adapter 接口。
- 未实现 CLI 命令分发。

功能：

- 未实现 `detect`。
- 未实现 `extract`。
- 未实现 JSONL 读写。
- 未实现译文校验。
- 未实现翻译包构建。
- 未实现 runtime 插件模板。
- 未实现 TyranoScript `.ks` 解析。

测试：

- 未配置测试框架。
- 未添加单元测试。
- 未添加样例 fixtures。

发布：

- 未配置 npm 发布流程。
- 未配置 CI。
- 未配置版本发布说明。

## 7. 当前命令

安装依赖：

```bash
npm install
```

类型检查：

```bash
npm run typecheck
```

构建：

```bash
npm run build
```

开发运行：

```bash
npm run dev
```

查看 git 状态：

```bash
git status --short --branch --ignored
```

## 8. 本地注意事项

- `node_modules/`、`dist/`、`.npm-cache/` 已忽略。
- `游戏逆向报告/` 已忽略。
- 本地逆向报告可以继续用于开发分析，但不要提交。
- 如果 npm 在受限环境中失败，先看 `--loglevel=verbose` 输出。
- 当前 npm registry 可能走本机代理，受限沙箱中可能需要网络权限。

## 9. 下一步建议

下一次开工建议按这个顺序推进：

1. 创建最小项目骨架目录。
2. 定义 Core 层模型：`Result<T>`、`ExtractedTextEntry`、`TranslationEntry`。
3. 定义 `GameEngineAdapter` 接口。
4. 实现 `detect` 命令的最小路径。
5. 为 TyranoScript adapter 添加只读检测逻辑。
6. 再进入 `.ks` 文本提取和 JSONL 导出。
